name: Publish to npm

on:
  pull_request:
    types: [closed]

jobs:
  publish:
    runs-on: ubuntu-latest
    if: contains( github.event.pull_request.labels.*.name, '[ Type ] NPM version update' ) && startsWith( github.head_ref, 'release/') && github.event.pull_request.merged == true
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - name: Publish package to npm
        uses: Automattic/vip-actions/npm-publish@c3e7cf188f63a76685b1495a237828845c3ae531
        with:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USE_TRUSTED_PUBLISHING: 'true'
          node-version: 'lts/*'
